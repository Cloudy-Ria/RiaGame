namespace StateMachine.TriggersAndZones
{
    /// <summary>
    /// Тип движения зоны обнаружения
    /// </summary>
    public enum DetectionZoneMovementType
    {
        Moving,  // Зона движется вместе с врагом
        Static   // Зона статична (зафиксирована в начальной позиции)
    }
}

